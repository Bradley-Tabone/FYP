# =============================================================================
# FYP - ML Training Environment Requirements
# =============================================================================
# Python 3.11-3.13 recommended (developed on 3.13).
#
# ── INSTALL ──────────────────────────────────────────────────────────────────
#   1. (Recommended) create + activate a virtual environment:
#          python -m venv .venv
#          .venv\Scripts\activate          (Windows)
#          source .venv/bin/activate        (Linux/macOS)
#
#   2. GPU users (NVIDIA, CUDA 12.x) — install PyTorch FIRST from the CUDA index,
#      otherwise pip pulls the CPU-only build:
#          pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu128
#
#      CPU-only users can skip this step (the plain torch below will install).
#
#   3. Install everything else:
#          pip install -r requirements_fyp.txt
# =============================================================================

# ── Deep-learning frameworks ────────────────────────────────────────────────
torch==2.8.0
torchvision==0.23.0
tensorflow==2.20.0
tf_keras==2.20.1

# ── Object-detection model libraries ────────────────────────────────────────
ultralytics==8.4.14          # YOLO + RT-DETR training/inference
rfdetr==1.4.3                # RF-DETR (Nano/Base)
transformers==4.57.6
peft==0.18.1

# ── ONNX / TFLite export pipeline ───────────────────────────────────────────
onnx==1.19.1
onnxruntime-gpu==1.23.2      # CPU-only users: replace with  onnxruntime==1.23.2
onnx2tf==1.28.8
onnxslim==0.1.86
onnx_graphsurgeon==0.5.8
sng4onnx==2.0.1
ai-edge-litert==2.1.2        # TFLite interpreter used for validation

# ── Data, augmentation & image processing ───────────────────────────────────
albumentations==2.0.8
opencv-python==4.13.0.92
ImageHash==4.3.2
roboflow==1.2.14             # dataset download
pycocotools==2.0.11          # COCO-format evaluation

# ── Scientific / analysis stack ─────────────────────────────────────────────
# Left unpinned so pip resolves a set compatible with torch/tensorflow above.
numpy
pandas
scikit-learn
matplotlib
seaborn
pillow
PyYAML

# ── Notebook environment ────────────────────────────────────────────────────
ipykernel==7.2.0             # enough to run the .ipynb files in VS Code
# jupyterlab                 # uncomment if you want a standalone browser notebook server
